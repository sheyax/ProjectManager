{
    "name": "mock-server",
    "description": "A mock server for homework",
    "scripts": {
        "start": "json-server --watch db.json --port 3002"
    },
    "dependencies": {
        "json-server": "^0.17.0"
    }
}